import React from 'react';
import { WorkWithUsButton as StyledButton } from './styles';

const WorkWithUsButton = ({ onClick }) => {
  return <StyledButton onClick={onClick}>Work With Us</StyledButton>;
};

export default WorkWithUsButton;
